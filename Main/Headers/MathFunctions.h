#ifndef MATHFUNCTIONS_H_
#define	MATHFUNCTIONS_H_

double darbuVidurkis(const std::vector<int>&);  // calculates and returns the arithmetic mean of the vector elements;
double darbuMediana(const std::vector<int>);  // creates placeholder vector, sorts it, finds and returns the median of sorted values;

#endif